export interface IUserProps {
  role?: string[];
  data?: {
    // displayName?: string;
    // photoURL?: string;
    // email?: string;
    // shortcuts?: string[];
    // settings?: Partial<FuseSettingsProps>;
  };
  loginRedirectUrl?: string;
}
